import { SectionButton } from "../../styles/sectionbutton.style";


const SectionButtonComponent = ({text}) => {
  return(
    <SectionButton.Wrapper>
      <SectionButton.Text>
        1. Checkout k8s courses
      </SectionButton.Text>
    </SectionButton.Wrapper>
  )
}

export default SectionButtonComponent;